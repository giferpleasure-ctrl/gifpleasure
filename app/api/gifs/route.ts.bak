// import { NextResponse } from "next/server";
// import { supabaseAdmin } from "@/lib/supabase";
// import metadata from "@/public/gifs/metadata.json";

// export async function GET() {
//   const { data: stats } = await supabaseAdmin
//     .from("gif_stats")
//     .select("gif_id, likes, views");

//   const statsMap: Record<string, { likes: number; views: number }> = {};
//   stats?.forEach((s) => {
//     statsMap[s.gif_id] = { likes: s.likes, views: s.views };
//   });

//   const gifs = metadata.map((gif) => ({
//     ...gif,
//     likes: statsMap[gif.id]?.likes ?? gif.likes,
//     views: statsMap[gif.id]?.views ?? gif.views,
//   }));

//   return NextResponse.json(gifs);
// }
